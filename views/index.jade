extends layout

block content
  h1= title
  p Welcome to #{title}
  div.message-board
    each message in messages 
      div.message-card 
        h2= message.title
        p.description= message.text
        p.username= message.user
        p.date= message.date